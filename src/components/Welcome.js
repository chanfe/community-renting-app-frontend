import React from 'react';

const Welcome = (props) => {
  // console.log(props)
  return (
    <div>
      <h1>Welcome {props.name.username} this is the</h1>
    </div>
  );
};

export default Welcome;
